# TrajProxy 测试用例目录

**文档版本**: v1.3  
**创建日期**: 2026-01-05  
**最后更新**: 2026-01-05  
**适用版本**: 当前主分支

---

## 1. 分类体系

### 1.1 测试类型定义

| 系列 | 类型 | 职责 | 数量 |
|------|------|------|------|
| **C1xx** | OpenAI 格式一致性 | 验证 OpenAI 兼容接口与 vLLM 响应的逐字段一致性 | 7 |
| **C2xx** | Claude 格式一致性 | 验证 Claude 接口适配与 vLLM 响应的逐字段一致性 | 7 |
| **F1xx** | 接口差异 + 基础API | 验证模型管理、参数透传、错误处理等基础功能 | 19 |
| **F2xx** | 轨迹存储验证 | 验证存储的轨迹字段格式与请求/响应内容一致性 | 11 |
| **F3xx** | TITO 多轮缓存 | 验证 TokenPipeline 多轮对话的 PrefixMatchCache 命中行为 | 11 |
| **P1xx** | 性能测试 | 长稳、并发、流式压测 | 3 |
| **A1xx** | 归档测试 | 独立归档进程的存储与恢复 | 4 |
| **总计** | | | **62** |

### 1.2 矩阵标签说明

**Pipeline 类型**:
- `Direct`: 直接转发模式（token_in_token_out=false），无 Parser 参与
- `TITO`: Token 进出模式（token_in_token_out=true），可有 Parser 配置

**响应模式**:
- `ns`: 非流式（nonstreaming）
- `s`: 流式（streaming）
- `(ns+s)`: 同一用例覆盖两种模式

**Parser 配置**（仅 TITO 模式可用）:
- `无`: 无 Parser
- `Tool`: 配置 Tool Parser（默认 qwen3_coder）
- `Reasoning`: 配置 Reasoning Parser（默认 qwen3）
- `T+R`: 同时配置 Tool + Reasoning Parser（默认 qwen3_coder + qwen3）

**轮次**:
- `单轮`: 单次请求
- `多轮(n轮)`: n 轮连续对话，涉及前缀缓存

**参数变体**（融入多轮或作为多轮用例的子场景）:
- `kwargs`: chat_template_kwargs 变体（preserve_thinking=true 为默认，其他值在多轮中验证）
- `tool_choice`: 工具选择模式（auto/required/named），融入多轮 Tool 用例

**重要默认值**:
- `preserve_thinking=true`（所有用例默认）
- `tool_choice=auto`（无特殊标注时）

### 1.3 来源标注

| 标注 | 含义 |
|------|------|
| `保留` | 现有用例，保持原编号 |
| `迁移` | 从其他系列迁移过来，重新编号 |
| `新增` | 全新补充的用例 |
| `删除` | 冗余移除 |
| `合并` | 与其他用例合并 |
| `降级` | 功能验证降级为冒烟测试 |

---

## 2. C1xx: OpenAI 格式一致性

**验证目标**: 使用 `compare.py` 对比 trajproxy 与 vLLM 的 OpenAI 兼容接口响应，确保逐字段一致性、禁止特殊 token 泄漏。

**矩阵维度**: Pipeline × Parser × 轮次（Direct 无 Parser，TITO 按 Parser 组合展开；所有用例统一 `(ns+s)` 覆盖两种模式）

**设计原则**:
- 单轮用例（C101-C105）验证各 Parser 组合的基础格式一致性
- 多轮用例（C106-C107）同时验证多轮格式一致性 + 前缀缓存递增，**内嵌参数变体**（kwargs、tool_choice）作为多轮场景的子验证
- `preserve_thinking=true` 作为全局默认值，不再单列反向用例

| 编号 | 名称 | 矩阵标签 | 来源 | 原编号 | 说明 |
|------|------|---------|------|--------|------|
| **C101** | Direct 纯文本一致性 | `Direct×(ns+s)×无×单轮×OpenAI` | 保留 | 原C300 | Direct 基线：透传模式一致性 |
| **C102** | TITO 纯文本一致性 | `TITO×(ns+s)×无×单轮×OpenAI` | 保留 | 原C301 | TITO 基线：Token 编解码正确性 |
| **C103** | TITO Tool 一致性 | `TITO×(ns+s)×Tool×单轮×OpenAI`（默认 auto） | 保留 | 原C302 | tool_calls 结构 + arguments 禁止 token |
| **C104** | TITO Reasoning 一致性 | `TITO×(ns+s)×Reasoning×单轮×OpenAI`（默认 preserve=true） | 保留 | 原C303 | reasoning 字段一致性 + content 剥离 `</think>` |
| **C105** | TITO Reasoning+Tool 一致性 | `TITO×(ns+s)×T+R×单轮×OpenAI`（默认 preserve=true, auto） | 保留 | 原C304 | 全字段：reasoning + tool_calls 逐项对比 |
| **C106** | 多轮 T+R 非流式一致性 | `TITO×ns×T+R×3轮×OpenAI`（含 kwargs/tool_choice 变体子场景） | **新增** | - | 每轮对比 vLLM；同时验证缓存递增。子场景：(a) tool_choice=required 强制调用, (b) tool_choice=named 指定工具, (c) enable_thinking=false 关闭推理 |
| **C107** | 多轮 T+R 流式一致性 | `TITO×s×T+R×3轮×OpenAI`（含 kwargs/tool_choice 变体子场景） | **新增** | - | 每轮流式重建后对比；同时验证流式缓存递增。子场景同 C106 |

---

## 3. C2xx: Claude 格式一致性

**验证目标**: 验证 Claude `/v1/messages` 端点的 LiteLLM 适配正确性，使用 `compare.py` 对比一致性。

**对等原则**: C2 与 C1 在核心维度（Pipeline × Parser × 轮次）保持严格对等。Claude 不支持 `tool_choice` 的 required/named 变体，因此 C2 多轮用例不包含 kwargs/tool_choice 子场景（由 C1 多轮覆盖）。

| 编号 | 名称 | 矩阵标签 | 来源 | 原编号 | 说明 |
|------|------|---------|------|--------|------|
| **C201** | Claude Direct 一致性 | `Direct×(ns+s)×无×单轮×Claude` | **迁移+升级** | 原F101/F109 | 合并原 F101(ns)+F109(s)，升级为 compare.py 对比 |
| **C202** | Claude TITO 纯文本一致性 | `TITO×(ns+s)×无×单轮×Claude` | **新增** | - | 对等 C102 |
| **C203** | Claude TITO Tool 一致性 | `TITO×(ns+s)×Tool×单轮×Claude` | **新增** | - | 对等 C103 |
| **C204** | Claude TITO Reasoning 一致性 | `TITO×(ns+s)×Reasoning×单轮×Claude` | **新增** | - | 对等 C104 |
| **C205** | Claude TITO Reasoning+Tool 一致性 | `TITO×(ns+s)×T+R×单轮×Claude` | **迁移+升级** | 原F110/F111 | 合并原 F110(ns)+F111(s)，升级为 compare.py 对比。对等 C105 |
| **C206** | Claude 多轮 T+R 非流式一致性 | `TITO×ns×T+R×3轮×Claude` | **新增** | - | 对等 C106（不含 kwargs/tool_choice 子场景）|
| **C207** | Claude 多轮 T+R 流式一致性 | `TITO×s×T+R×3轮×Claude` | **新增** | - | 对等 C107（不含 kwargs/tool_choice 子场景）|

---

## 4. F1xx: 接口差异 + 基础API

**验证目标**: 验证模型管理、参数透传/过滤、错误响应、Processor 管理等基础功能。

**组织顺序**: 模型管理(5) → 错误处理(2) → 参数透传(4) → Processor 管理(4) → 自定义 Parser(2) → TITO 冒烟(2)，编号连续。

### 4.1 模型管理 (F101-F105)

| 编号 | 名称 | 矩阵标签 | 来源 | 原编号 | 说明 |
|------|------|---------|------|--------|------|
| **F101** | 模型 CRUD | `基础API×模型注册` | 保留 | 原F200 | 注册→列表→删除→列表 |
| **F102** | PANGU 格式 | `基础API×PANGU兼容` | 保留 | 原F201 | `{model_name},{run_id}` 兼容 |
| **F103** | 重复注册模型 | `基础API×模型管理×幂等` | **新增** | - | 注册同一 (run_id, model_name) → 409 或覆盖 |
| **F104** | 删除预置模型 | `基础API×模型管理×保护` | **新增** | - | DELETE 预置模型 → 拒绝/返回 false |
| **F105** | 模型未注册 | `基础API×模型管理×查询` | **新增** | - | 请求未注册模型 → 404 |

### 4.2 错误处理 (F106-F107)

| 编号 | 名称 | 矩阵标签 | 来源 | 原编号 | 说明 |
|------|------|---------|------|--------|------|
| **F106** | 无效 model 参数 | `基础API×参数校验×422` | **新增** | - | model="" → 422, model=null → 422 |
| **F107** | 并发限流 | `系统级×并发控制×429` | **新增** | - | 超出 max_concurrent_requests → 429 |

### 4.3 参数透传与过滤 (F108-F111)

| 编号 | 名称 | 矩阵标签 | 来源 | 原编号 | 说明 |
|------|------|---------|------|--------|------|
| **F108** | 参数透传 (Direct) | `基础API×参数透传×Direct` | 保留 | 原F210 | Mock 验证 header/参数透传 |
| **F109** | 参数过滤 (TITO) | `基础API×参数过滤×TITO` | 保留 | 原F211 | TITO 模式黑名单过滤 |
| **F110** | logprobs/token_ids 强制覆盖 | `基础API×参数强制覆盖×TITO` | 保留 | 原F214 | Proxy 强制覆盖 + 客户端过滤 |
| **F111** | chat_template_kwargs 透传与消费 | `基础API×kwargs×Direct/TITO` | 保留 | 原F220 | Direct 透传 vs TITO 消费 |

### 4.4 Processor 管理 (F112-F115)

| 编号 | 名称 | 矩阵标签 | 来源 | 原编号 | 说明 |
|------|------|---------|------|--------|------|
| **F112** | Processor 懒加载 | `系统级×Processor×懒加载` | 保留 | 原F215 | 首次请求懒加载 + 缓存命中 |
| **F113** | Processor LRU 命中 | `系统级×Processor×LRU` | 保留 | 原F216 | 多模型交替请求 |
| **F114** | 预置模型懒加载 | `系统级×Processor×预置` | 保留 | 原F217 | config.yaml 预置模型 |
| **F115** | LRU 淘汰重加载 | `系统级×Processor×LRU淘汰` | 保留 | 原F218 | 35模型超32上限 |

### 4.5 自定义 Parser 发现 (F116-F117)

| 编号 | 名称 | 矩阵标签 | 来源 | 原编号 | 说明 |
|------|------|---------|------|--------|------|
| **F116** | 自定义 ToolParser 发现 | `系统级×Parser×热加载` | 保留 | 原F212 | 热加载 parser |
| **F117** | 自定义 ReasoningParser 发现 | `系统级×Parser×热加载` | 保留 | 原F213 | 热加载 parser |

### 4.6 TITO 基础冒烟 (F118-F119)

| 编号 | 名称 | 矩阵标签 | 来源 | 原编号 | 说明 |
|------|------|---------|------|--------|------|
| **F118** | TITO Token 模式基础 | `TITO×ns×单轮×冒烟` | 保留 | 原F104 | TITO 基础请求 + response_ids 验证 |
| **F119** | TITO Token 流式基础 | `TITO×s×单轮×冒烟` | 保留 | 原F105 | TITO 流式基础请求 |

---

## 5. F2xx: 轨迹存储验证

**验证目标**: 验证存储的轨迹字段格式正确，且与实际请求/响应内容一致。

**组织顺序**: 基础轨迹(4) → 存储一致性(2) → 特殊模式(2) → 查询API(2) → 交叉验证(1)，编号连续。

### 5.1 基础轨迹字段 (F201-F204)

| 编号 | 名称 | 矩阵标签 | 来源 | 原编号 | 说明 |
|------|------|---------|------|--------|------|
| **F201** | 轨迹捕获基础 | `TITO×ns×单轮×轨迹` | 保留 | 原F103 | 请求后查询 /trajectory 确认记录存在 |
| **F202** | EOS 一致性 | `TITO×ns×单轮×EOS校验` | 保留 | 原F114 | full_conv_text 与 full_conv_token_ids 的 EOS 一致 |
| **F203** | 基础 chat 轨迹存储 | `Direct×ns×单轮×冒烟` | **降级** | 原F100 | nginx 层请求 → 轨迹字段存在+非空（轻量冒烟） |
| **F204** | 流式 chat 轨迹存储 | `Direct×s×单轮×冒烟` | **降级** | 原F102 | nginx 层流式请求 → 轨迹存储（轻量冒烟） |

### 5.2 流式/非流式存储一致性 (F205-F206)

| 编号 | 名称 | 矩阵标签 | 来源 | 原编号 | 说明 |
|------|------|---------|------|--------|------|
| **F205** | TITO 流式/非流式轨迹一致性 | `TITO×(ns+s)×单轮×存储对比` | 保留 | 原F206 | 两种模式的存储字段比较 |
| **F206** | Direct 流式/非流式轨迹一致性 | `Direct×(ns+s)×单轮×存储对比` | 保留 | 原F207 | Direct 模式两种存储比较 |

### 5.3 特殊模式轨迹 (F207-F208)

| 编号 | 名称 | 矩阵标签 | 来源 | 原编号 | 说明 |
|------|------|---------|------|--------|------|
| **F207** | PANGU + 轨迹存储 | `PANGU×ns×单轮×轨迹` | 保留 | 原F208 | PANGU 格式的轨迹完整性 |
| **F208** | Tool 场景轨迹存储 | `TITO×Tool×单轮×轨迹` | **新增** | - | tool_calls 和 assistant 含 tool_calls 的完整记录 |

### 5.4 轨迹查询 API (F209-F210)

| 编号 | 名称 | 矩阵标签 | 来源 | 原编号 | 说明 |
|------|------|---------|------|--------|------|
| **F209** | Trajectories API | `轨迹API×查询` | **合并** | 原F112+原F209 | GET /trajectories?run_id=（合并两层） |
| **F210** | trajectory fields 过滤 | `轨迹API×fields过滤` | 保留 | 原F219 | ?fields= 包含/排除语法 |

### 5.5 轨迹字段交叉验证 (F211)

| 编号 | 名称 | 矩阵标签 | 来源 | 原编号 | 说明 |
|------|------|---------|------|--------|------|
| **F211** | 轨迹字段与请求内容交叉验证 | `TITO×ns×单轮×交叉校验` | **新增** | - | 存储的 messages/token_ids 与实际请求一致 |

---

## 6. F3xx: TITO 多轮缓存

**验证目标**: 验证 TokenPipeline 多轮对话的 PrefixMatchCache 命中行为。

**组织顺序**: 正常缓存命中(4) → 扩展Parser组合(2) → 混合模式(2) → 异常/边界条件(3)，编号连续。

**核心校验公式**:
- **校验1**: `cache_hit_tokens[R_n]` == `len(full_conversation_token_ids[R_{n-1}])`
- **校验2**: `len(full_conversation_token_ids)` == `len(token_ids)` + `len(response_ids)`

### 6.1 正常缓存命中路径 (F301-F304)

| 编号 | 名称 | 矩阵标签 | 来源 | 原编号 | 说明 |
|------|------|---------|------|--------|------|
| **F301** | 基线缓存：纯多轮 | `TITO×ns×无×3轮×session=有` | 保留 | 原F106 | 纯文本 3 轮递增匹配基线 |
| **F302** | 多轮 Tool 缓存 | `TITO×ns×Tool×2轮×session=有` | 保留 | 原F113 | tool_calls 回传后模板渲染匹配 |
| **F303** | 多轮 T+R 非流式缓存 | `TITO×ns×T+R×3轮×session=有` | 保留 | 原F115 | reasoning+tool 组合回传匹配 |
| **F304** | 多轮 T+R 流式缓存 | `TITO×s×T+R×3轮×session=有` | 保留 | 原F116 | 流式推理+工具组合回传匹配 |

### 6.2 扩展 Parser 组合 (F305-F306)

| 编号 | 名称 | 矩阵标签 | 来源 | 原编号 | 说明 |
|------|------|---------|------|--------|------|
| **F305** | 多轮纯 Reasoning 缓存 | `TITO×ns×Reasoning×3轮×session=有` | **新增** | - | reasoning_content 回传匹配 |
| **F306** | 流式存储缓存 | `TITO×s×无×3轮×session=有` | **新增** | - | 流式存储的轨迹可被下轮命中 |

### 6.3 混合模式 (F307-F308)

| 编号 | 名称 | 矩阵标签 | 来源 | 原编号 | 说明 |
|------|------|---------|------|--------|------|
| **F307** | 流式+非流式交替缓存 | `TITO×交替(s→ns→s)×T+R×3轮×session=有` | **新增** | - | 跨存储模式缓存兼容 |
| **F308** | Reasoning+Tool 冒烟 | `TITO×(ns+s)×T+R×单轮×冒烟` | **降级** | 原F107+原F108 | 从功能验证降级为单轮冒烟（不验缓存），覆盖流式+非流式 |

### 6.4 异常/边界条件 (F309-F311)

| 编号 | 名称 | 矩阵标签 | 来源 | 原编号 | 说明 |
|------|------|---------|------|--------|------|
| **F309** | 无 session 缓存跳过 | `TITO×ns×Tool×2轮×session=无` | **新增** | - | cache_hit_tokens 恒为 0 |
| **F310** | kwargs 变化缓存失效 | `TITO×ns×T+R×2轮×session=有×kwargs=thinking=false变` | **新增** | - | kwargs 改变→缓存不匹配 |
| **F311** | tools 变更缓存失效 | `TITO×ns×Tool×2轮×session=有×tools=不同` | **新增** | - | tools 定义改变→缓存不匹配 |

---

## 7. P1xx: 性能测试

**验证目标**: 长稳、并发、流式压测（保持不变）。

| 编号 | 名称 | 矩阵标签 | 来源 | 原编号 | 说明 |
|------|------|---------|------|--------|------|
| **P100** | 长稳测试 | `性能×100请求×延迟统计` | 保留 | 原P100 | 持续发送 100 个请求 |
| **P101** | 并发测试 | `性能×10并发×100请求` | 保留 | 原P101 | 维持 10 个并发发送 100 个请求 |
| **P102** | 流式并发压测 | `性能×100并发×流式` | 保留 | 原P102 | 100 并发流式请求 |

---

## 8. A1xx: 归档测试

**验证目标**: 独立归档进程的存储与恢复（保持不变）。

| 编号 | 名称 | 矩阵标签 | 来源 | 原编号 | 说明 |
|------|------|---------|------|--------|------|
| **A100** | 归档进程配置 | `归档×配置验证` | 保留 | 原A100 | 验证独立归档进程正确启动 |
| **A101** | 手动归档 | `归档×手动触发×run_id粒度` | 保留 | 原A101 | 按 run_id 粒度归档 |
| **A102** | 定时归档 | `归档×定时×未过期保留` | 保留 | 原A102 | 验证 run 内有活跃记录时保留 |
| **A103** | 归档数据恢复 | `归档×数据恢复×完整性` | 保留 | 原A103 | 验证归档文件可读、JSONL 格式正确 |

---

## 9. 删除与合并清单

| 原编号 | 处理方式 | 去向 | 原因 |
|--------|---------|------|------|
| **原F100** | 降级 | **F203** | 功能验证被 C101 覆盖，降级为轨迹冒烟 |
| **原F102** | 降级 | **F204** | 功能验证被 C101 流式覆盖，降级为轨迹冒烟 |
| **原F101** | 合并+迁移 | C201 | 与 F109 合并为 Claude Direct 一致性对比 |
| **原F109** | 合并+迁移 | C201 | 与 F101 合并为 Claude Direct 一致性对比 |
| **原F110** | 合并+迁移 | C205 | 与 F111 合并为 Claude T+R 一致性对比 |
| **原F111** | 合并+迁移 | C205 | 与 F110 合并为 Claude T+R 一致性对比 |
| **原F107** | 降级 | **F308** | 与 F108 合并，降级为单轮冒烟 |
| **原F108** | **删除** | 并入 F308 | 流式部分并入 F308，与 F107 合并 |
| **原F112** | 合并 | **F209** | 与 F209 合并为统一的 Trajectories API 测试 |
| **原F209** | 合并 | **F209** | 与 F112 合并 |
| **原F202** | **删除** | C103 覆盖 | Proxy层 Tool 单轮功能被 C103 完全覆盖 |
| **原F203** | **删除** | C104 覆盖 | Proxy层 Reasoning 单轮功能被 C104 完全覆盖 |
| **原F204** | **删除** | C103 流式覆盖 | Proxy层 Tool 流式功能被 C103 流式部分完全覆盖 |
| **原F205** | **删除** | C104 流式覆盖 | Proxy层 Reasoning 流式功能被 C104 流式部分完全覆盖 |

> 注: 除上述处理外，F1xx/F2xx/F3xx 内部的编号在 v1.1-v1.3 中**全部重排**以保证连续无跳跃，原编号保留在表格"原编号"列供追溯。

---

## 10. 矩阵覆盖分析

### 10.1 C1/C2 对等矩阵

| 核心维度 | C1 (OpenAI) | C2 (Claude) | 对等? |
|------|------------|------------|-------|
| Direct 纯文本（ns+s） | C101 ✅ | C201 ✅ | ✅ |
| TITO 纯文本（ns+s） | C102 ✅ | C202 ✅ | ✅ |
| TITO Tool（ns+s） | C103 ✅ | C203 ✅ | ✅ |
| TITO Reasoning（ns+s） | C104 ✅ | C204 ✅ | ✅ |
| TITO T+R（ns+s） | C105 ✅ | C205 ✅ | ✅ |
| TITO 多轮 T+R 非流式 | C106 ✅ | C206 ✅ | ✅ |
| TITO 多轮 T+R 流式 | C107 ✅ | C207 ✅ | ✅ |

**C1 多轮独有子场景**（Claude 不支持，不算破坏对等）:
- tool_choice=required 强制调用（qwen3_coder 专属路径） → C106-a
- tool_choice=named 指定工具（qwen3_xml 专属路径） → C106-b
- enable_thinking=false 推理关闭（qwen3 专属 kwargs） → C106-c

### 10.2 F2xx 轨迹存储矩阵覆盖

| 维度 | Pipeline | 流式 | Parser | 存储一致性 | API验证 | 覆盖 |
|------|---------|------|--------|----------|--------|------|
| F201 | TITO | ns | 无 | — | — | ✅ |
| F202 | TITO | ns | 无 | EOS 校验 | — | ✅ |
| F203 | Direct | ns | 无 | — | — | ✅ (降级冒烟) |
| F204 | Direct | s | 无 | — | — | ✅ (降级冒烟) |
| F205 | TITO | (ns+s)对比 | 无 | ✅ 跨模式 | — | ✅ |
| F206 | Direct | (ns+s)对比 | 无 | ✅ 跨模式 | — | ✅ |
| F207 | PANGU | ns | 无 | — | — | ✅ |
| F208 | TITO | ns | Tool | — | — | 🆕 |
| F209 | — | — | — | — | GET /trajectories | ✅ |
| F210 | — | — | — | — | fields= 过滤 | ✅ |
| F211 | TITO | ns | 无 | 字段交叉验证 | — | 🆕 |

**F2xx 覆盖率**: 11/11 = 100%

### 10.3 F3xx 缓存矩阵覆盖

| 编号 | 流式 | Parser | 轮次 | session | kwargs变体 | tools变更 | 覆盖 |
|------|------|--------|------|---------|-----------|----------|------|
| F301 | ns | 无 | 3轮 | 有 | — | — | ✅ |
| F302 | ns | Tool | 2轮 | 有 | — | 同tools | ✅ |
| F303 | ns | T+R | 3轮 | 有 | — | 同tools | ✅ |
| F304 | s | T+R | 3轮 | 有 | — | 同tools | ✅ |
| F305 | ns | Reasoning | 3轮 | 有 | — | — | 🆕 |
| F306 | s | 无 | 3轮 | 有 | — | — | 🆕 |
| F307 | 混合(s→ns→s) | T+R | 3轮 | 有 | — | 同tools | 🆕 |
| F308 | **(ns+s)** | T+R | 单轮 | 有 | — | — | ✅ (降级冒烟) |
| F309 | ns | Tool | 2轮 | **无** | — | — | 🆕 |
| F310 | ns | T+R | 2轮 | 有 | **thinking=false 变** | — | 🆕 |
| F311 | ns | Tool | 2轮 | 有 | — | **tools 不同** | 🆕 |
| C106/7 | ns/s | T+R | 3轮 | 有 | **required/named/thinking=false** | 同tools | 🆕 |

**F3xx+C1xx 多轮覆盖率**: 11/11 = 100%

### 10.4 F1xx 基础API 覆盖

| 子组 | 编号范围 | 数量 | 覆盖状态 |
|------|---------|------|---------|
| 模型管理 | F101-F105 | 5 | ✅ 含 CRUD + 重复注册 + 删除预置 + 未注册 |
| 错误处理 | F106-F107 | 2 | ✅ 含参数校验(422) + 限流(429) |
| 参数透传 | F108-F111 | 4 | ✅ Direct透传 + TITO过滤 + 强制覆盖 + kwargs |
| Processor 管理 | F112-F115 | 4 | ✅ 懒加载 + LRU + 预置 + 淘汰 |
| 自定义Parser | F116-F117 | 2 | ✅ Tool + Reasoning 热加载 |
| TITO 冒烟 | F118-F119 | 2 | ✅ ns + s 基础请求 |

---

## 11. 实施建议

### 11.1 优先级排序

**Phase 1: 基础迁移（高优先级，1-2 天）**
1. 迁移升级 C201（合并原 F101+F109）→ Claude Direct 对比
2. 迁移升级 C205（合并原 F110+F111）→ Claude TITO T+R 对比
3. 新增 C202/C203/C204（补齐 Claude TITO 基础）

**Phase 2: 多轮核心（高优先级，2-3 天）**
1. 新增 C106/C107（OpenAI 多轮 T+R 一致性 + 缓存）
2. 新增 C206/C207（Claude 多轮 T+R 一致性）
3. 新增 F306/F307/F309（缓存关键场景）

**Phase 3: 异常路径 + 系统加固（中优先级，2-3 天）**
1. 新增 F308/F310/F311（缓存异常路径）
2. 新增 F115-F119（基础API错误处理）
3. 新增 F211/F212（轨迹存储交叉验证）

### 11.2 资源估算

- **新增用例**: 18 个（较原方案减少 6 个）
- **迁移升级**: 4 个
- **降级**: 3 个 (原F100/F102/F107)
- **合并**: 3 个 (原F101+F109→C201, 原F110+F111→C205, 原F112+F209→F209)
- **删除**: 5 个 (原F108/原F202/原F203/原F204/原F205)
- **净增量**: +11 个用例（原48个→目标62个，但删除5个后为57个）

预计工作量：
- Phase 1: 1-2 天
- Phase 2: 2-3 天
- Phase 3: 2-3 天

---

## 12. 变更记录

| 版本 | 日期 | 变更内容 |
|------|------|---------|
| v1.0 | 2026-01-05 | 初始版本，基于 48 个现有用例重构为 68 个分类用例 |
| v1.1 | 2026-01-05 | 修订 C1/C2 系列：移除伪对称（Direct 不展开 Parser 维度）；`preserve_thinking=true` 作为全局默认不再单列反向用例；`tool_choice` 变体融入多轮用例作为子场景；删除冗余的 C112；总数 68→62 |
| v1.2 | 2026-01-05 | **F 系列重排序**：按子组归类 + 组内由简到繁，编号连续无跳跃。F1xx 重分为 6 个子组（模型管理→错误处理→参数透传→Processor管理→自定义Parser→TITO冒烟）；F2xx 重分为 5 个子组（基础轨迹→存储一致性→特殊模式→查询API→交叉验证）；F3xx 重分为 4 个子组（正常缓存→扩展Parser→混合模式→异常边界）。新增 §10.2 F2xx 覆盖分析表 |
| v1.3 | 2026-01-05 | **审计原用例去向**：补全 5 个遗漏删除项（原F108 并入 F308，原F202-F205 被 C103/C104 覆盖删除）；F308 升级为 `(ns+s)` 同时覆盖流式+非流式；新增 §9.A 完整的 48 个原用例追溯表；总数 62→62（F308 扩展但数量不变）|
| v1.3 | 2026-01-05 | 修正资源估算：删除 5 个原用例，降级 3 个，合并 3 个；净增量 +11 个 |
